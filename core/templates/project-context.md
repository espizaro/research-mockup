# {{APP_NAME}} — Project Context

Generated by `setup.ps1` on {{DATE}}. The `research-mockup` skill reads this file at the
start of every task, so it always knows the project without being told again. Keep the
skill prompt short — the context lives here.

## Identity

- **App name:** {{APP_NAME}}
- **Mode:** {{MODE}}
- **Platform:** {{PLATFORM}}
- **Stack:** {{STACK}}
- **Primary language (UI copy):** {{LANG}}
- **Brand voice:** {{BRAND_VOICE}}

## Repositories

- **App repository:** {{APP_REPO_PATH}}{{APP_REPO_NOTE}}
- **Research instance (this repository):** {{INSTANCE_PATH}}

## Design system

- **Ownership:** {{DESIGN_OWNERSHIP}}
- **Token source:** {{TOKENS_PATH}} (auto-imported into `mockups/assets/tokens.css`)
- **Icon set:** {{ICON_SET}}
- **Typeface:** {{TYPEFACE}}
- **Design rules source:** `instance/project-rules.md`

## Architecture map

Auto-detected during setup; extend it as the app grows.

- Entry point(s):
- Router / navigation:
- Modules:
- Shared components:
- State / stores:
- i18n:

## Known surfaces and cross-references

Where each feature appears besides its main screen (home, notifications, links, chat,
timeline, etc.). Keep this up to date after every investigation.

## Decision log

| Date | Decision | Evidence / source |
|---|---|---|
| {{DATE}} | Project initialized | setup.ps1 |

> The skill updates this file after each research cycle. Treat it as the single source of
> truth for product context; do not duplicate this information in chat prompts.
